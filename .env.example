# Sentinel CCTV Integration System — Gateway Credentials
# These credentials authenticate against the RTSP gateway (103.250.160.189:8554 / stream.corp8.cloud).
# The '@' in your email will be automatically percent-encoded as '%40' by Sentinel.

RTSP_AUTH_EMAIL=your_email@example.com
RTSP_AUTH_PASSWORD=your_password
