opencv-python-headless==4.10.0.84
numpy
PyYAML==6.0.2

# Layer 2 — AI & Metadata Extraction
ultralytics==8.3.40
easyocr==1.7.2

# Layer 3 — Central Message Routing (Kafka)
confluent-kafka==2.6.1

# Layer 4 — Dashboard API & SQLite Consumer
fastapi==0.115.6
uvicorn[standard]==0.34.0
